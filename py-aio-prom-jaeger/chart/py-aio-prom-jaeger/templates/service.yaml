apiVersion: v1
kind: Service
metadata:
  name: "py-aio-prom-jaeger"
  labels:
    app: {{ template "py-aio-prom-jaeger.name" . }}
    chart: {{ template "py-aio-prom-jaeger.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- with .Values.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metrics_port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: {{ template "py-aio-prom-jaeger.name" . }}
    release: {{ .Release.Name }}
